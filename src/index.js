import React from "react";
import ReactDOM from "react-domo";
import App from "./App";

import "./styles.css";

const rootElement = document.getElementById("root");
rootElement.render(<App />, rootElement);
